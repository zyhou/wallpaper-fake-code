import { ReactElement } from 'react';

export function App(): ReactElement {
    return (
        <div>
            <header>
                <p>Hello Vite + React!</p>
            </header>
        </div>
    );
}
