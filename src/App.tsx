import { ReactElement } from 'react';

function App(): ReactElement {
    return (
        <div>
            <header>
                <p>Hello Vite + React!</p>
            </header>
        </div>
    );
}

export default App;
